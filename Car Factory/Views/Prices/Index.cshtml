@model IEnumerable<Car_Factory.Models.Price>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CarPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Car_ID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CarName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CarPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Car_ID.CarID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PriceID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PriceID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PriceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
